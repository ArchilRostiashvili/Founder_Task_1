
using UnityEngine;

namespace BebiAnimationsTest
{
    public class BebiAnimatorCallbackTest : MonoBehaviour
    {
        //[SerializeField] private BebiAnimator _animator;

        private void Start()
        {
            /*
            _animator.Play("IntroAnimation",

            (animationClip) => 
            {
                Debug.Log("Animation clip part started");

                if(animationClip is DoMoveAnimation)
                {
                    ((DoMoveAnimation)animationClip).SetEndValue(new Vector3(3, 3, 0));
                }
            },
            () =>
            {
                Debug.Log("Finished animation part");
            }, (bool hasAnim) =>
            {
                Debug.Log("Finished animation");
            });
            */
        }
    }
}
